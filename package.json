{
  "name": "shaker-monorepo",
  "private": true,
  "workspaces": [
    "frontend-vite"
  ],
  "scripts": {
    "dev": "npm --workspace frontend-vite run dev",
    "dev:legacy": "npm --prefix frontend run dev",
    "build": "npm --workspace frontend-vite run build",
    "nx": "nx"
  },
  "devDependencies": {
    "nx": "^22.7.0"
  }
}
